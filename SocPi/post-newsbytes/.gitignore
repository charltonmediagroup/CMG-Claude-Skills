# Secrets — never commit.
secrets/*.json
!secrets/*.example

# Per-row state and downloaded media.
cache/*
!cache/.gitkeep

# Run reports.
runs/*
!runs/.gitkeep

# Python artefacts.
__pycache__/
*.pyc
*.pyo
.venv/
.mypy_cache/
.pytest_cache/
